export interface AssetElement {
    id: string
    name: string
    uid: string
    start: string|number 
    duration: string|number
    hasVideo: string|boolean 
    format: string 
    videoSources: string
}